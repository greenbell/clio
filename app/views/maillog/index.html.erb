<h1>maillog<%= " - #{params[:session]}" if params[:session] %></h1>
<%= image_tag @graph.dataurl if @graph %>
<%= render "form" %>
<% if @logs %>
  <%= paginate @logs %>
  <%= render "table" %>
  <%= paginate @logs %>
<% else %>
  該当するログがありませんでした
<% end %>
<%= javascript_include_tag :maillog %>
